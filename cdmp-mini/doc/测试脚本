 go test -timeout 30m -v 

 # 运行单个删除测试
go test -v -run TestUserForceDelete_RealConcurrent -timeout=10m

# 运行所有压力测试（包括创建和删除）
go test -v -run "TestUser.*Concurrent" -timeout=15m

# 带详细日志的输出
go test -v -run TestUserForceDelete_RealConcurrent -timeout=10m -args -v=2


./user_delete_stress -v concurrent=10 precreate=200 requests=20
总删除: 10 × 20 = 200 次

预创建: 200 个用户

详细模式: 启用

用途: 极限压力测试


# 登录获取 Token
curl -X POST "http://localhost:8088/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "Admin@2021"
  }'

  # 替换为实际获取的 Token
ACCESS_TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# 删除特定用户
curl -X DELETE \
  "http://localhost:8088/v1/users/johndoe/force" \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json"